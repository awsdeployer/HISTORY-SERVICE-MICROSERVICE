# Role for history-service: only read pods it needs
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: history-role
  namespace: ashapp
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "list"]
